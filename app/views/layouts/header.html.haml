- if current_supplier
  = link_to "Log out", destroy_supplier_session_url, :method => :delete, class: "btn btn-info"
- else
  = link_to "Admin", new_admin_session_url, :method => :delete, class: "btn btn-success"
  = link_to "Supplier", new_supplier_session_url, class: "btn btn-warning"